/**
 * 
 */
"use strict";

/*alert = shows a message.
prompt = shows a message asking the user to input text. 
	It returns the text or, if Cancel button or Esc is clicked, null.
confirm = shows a message and waits for the user to press 
	“OK” or “Cancel”. It returns true for OK and false for Cancel/Esc.*/

//a simple page
//one line, one action
let name = prompt('What is your name?', 'Taewon');
alert(name);